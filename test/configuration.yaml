default_config:

demo:

lovelace:
  mode: storage
  dashboards:
    lovelace-yaml:
      mode: yaml
      title: yaml
      filename: test/lovelace.yaml

input_number:
  test:
    min: 0
    max: 10

timer:
  test:
    duration: "00:01:00"
